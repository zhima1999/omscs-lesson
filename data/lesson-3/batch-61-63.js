export const batchPages = [
  {
    page: 61,
    title: "Dropout Implementation",
    sourcePoints: [
      "每个训练 iteration 都重新生成 Dropout 掩码",
      "图中把一组激活与 0、1 掩码逐元素相乘，得到本次保留的输出",
      "标准测试阶段不再随机屏蔽节点，训练与测试的尺度衔接见下一页"
    ],
    explanation: [
      "一、这页从想法走到实现。上一页说训练时随机关闭一些输出，你可能会以为需要真的拆掉网络里的连接。其实最简单的实现只需要一组随机的数字，叫 Mask，掩码。神经元、权重和网络结构都还在，只是在本次计算里，有些激活被乘成零。因此 Dropout 不是删除模型文件里的参数，也不是永久剪掉某一层。",
      "二、先把图里的符号翻译成人话。Activation，激活，是某个神经元完成计算后传给下一层的数值。例如 a11、a21、a31、a41 可以理解为这一层四个位置的输出。图里的乘号表示对应位置逐元素相乘：第一个输出只乘第一个掩码，第二个只乘第二个。不需要把四个激活和四个掩码做复杂的矩阵乘法，也不是把这四个输出最后加成一个数。",
      "三、亲手算一遍图中的机制。假设四个原始激活是 2、4、6、8，本次掩码是 0、1、0、1。相乘后得到 0、4、0、8。第三个神经元本来输出 6，现在这一轮不能通过这条路径影响下一层；第二个输出 4 则被保留。这里先按原始 Dropout 示意计算，不额外放大输出。常见框架里的训练时放大版本，会在第 62 页单独解释。",
      "四、为什么每次都重新抽掩码？Iteration 在这里指处理一批训练数据、计算梯度并更新参数的一次过程。下一次可能抽到 1、1、0、0，于是可用的输出组合又变了。如果从训练开始到结束都固定使用 0、1、0、1，我们实际上一直在训练同一个较小网络，没有体现 Dropout 不断改变可用信息通道的特点。普通实现通常还会给同一批中的不同样本抽不同掩码，而不是要求整批共享同一组。",
      "五、掩码的概率是什么意思？沿用 PPT 的约定，p 表示保留概率。若 p=0.5，每个位置有一半概率为 1，另一半为 0。四个位置独立抽取时，不保证每次恰好留下两个；可能留下一个、三个，甚至全部保留。所谓平均留下两个，是看很多次随机抽取的结果，不是每次硬性选两个。你现在只需要把概率理解成长期出现的比例，不必先使用抽象的期望公式。",
      "六、被屏蔽的路径这一轮怎样学习？某个激活乘以 0 后，该路径传回到它之前的梯度也会被乘以 0，因此本次经由这条路径的学习信号被暂时挡住。下一轮它被保留，就能重新参与计算和接收梯度。不能因此说相关权重永远不更新：参数可能还有别的计算路径，优化器也可能带动量或权重衰减。准确的表述是，本次被屏蔽路径的数据梯度为零，而不是所有相关参数必然完全不动。",
      "七、测试时为什么不继续抽？标准推理希望同一个输入得到稳定的结果，因此停止普通 Dropout 的随机屏蔽，使用完整网络。PyTorch 中通常通过 model.train() 和 model.eval() 切换训练、推理行为。补充一个很容易混淆的点：不记录梯度与切换推理模式不是同一件事。单独使用 no_grad() 并不会自动关闭仍处于训练模式的 Dropout，因为是否记录梯度和是否随机屏蔽是两个独立设置。",
      "八、这页现在要掌握什么？实现的核心不是神经元消失，而是激活乘随机掩码；随机性主要发生在训练阶段；每轮重新抽取；测试使用完整网络。但完整网络比训练中的部分网络接收更多输出，数值尺度可能不一致。因此还需要下一页的缩放处理。不要把本页的原始乘法示意直接当成所有框架最终采用的数值结果。"
    ],
    takeaway: "Dropout 用随机 0、1 掩码暂时屏蔽激活，每次训练重新抽取；测试通常使用完整网络，接下来还要解决尺度匹配。",
    formulas: [
      "Raw dropout: output = activation * mask",
      "[2, 4, 6, 8] * [0, 1, 0, 1] = [0, 4, 0, 8] (element by element)",
      "PPT convention: P(mask = 1) = p_keep; P(mask = 0) = 1 - p_keep"
    ],
    status: "taught"
  },
  {
    page: 62,
    title: "Inference with Dropout",
    sourcePoints: [
      "训练阶段平均保留 p * fan_in 个输入，测试阶段全部输入都有效",
      "原始 Dropout 可以在测试时把相应输出或连接权重乘保留概率 p，使尺度相近",
      "另一种方法是在训练时把保留的输出乘 1/p，即 inverted dropout，测试不再缩放"
    ],
    explanation: [
      "一、先看为什么需要这一页。假设下一层平时会接收十个神经元的输出，训练时 Dropout 平均只保留五个，测试时突然收到全部十个。即使每个输出没有变大，总体输入的典型尺度也可能变化。模型是在训练时的条件下调整参数的，推理时如果条件突变，就可能影响后续计算。因此缩放的目的不是让模型多猜几次，而是让训练和测试之间的平均数值尺度衔接起来。",
      "二、Fan-in 是什么？它指一个节点有多少条输入连接，不是网络总共几层，也不是一批有多少张图片。一个节点接收十个上游输出时，fan_in=10。若每个上游输出以 0.5 的概率保留，长期平均的有效输入数量是 0.5 * 10=5。但某一次仍可能留下四个或六个。PPT 的 p * fan_in 说的是平均数量，不能理解成每一轮都强制精确保留五条连接。",
      "三、只看一个数，就能理解缩放。原始激活为 2，保留概率为 0.5。原始 Dropout 在训练时一半机会传出 2，一半机会传出 0。很多次的平均贡献是 0.5 * 2 + 0.5 * 0=1。测试如果直接传出完整激活 2，这个位置的贡献就比训练的平均贡献大了。这里算平均是为了比较尺度，不是说每次训练都真的传出 1。实际这一轮仍是 2 或 0。",
      "四、第一种方案：训练不放大，测试缩小。刚才训练平均贡献是 1，所以测试时把完整激活 2 乘保留概率 0.5，得到 1，就与训练的平均贡献一致。对接收这些激活的线性连接，也可以把相应连接权重乘 p，得到同样的线性缩放效果。课件把它写成 W_test=pW。这里指与这些被随机屏蔽激活相连的权重，不是要求不分层次地把模型中的所有参数都乘一次 p。",
      "五、第二种方案：训练放大，测试保持原样。这叫 Inverted dropout，反向缩放式 Dropout。仍然用激活 2、保留概率 0.5：保留时传出 2/0.5=4，屏蔽时传出 0。长期平均是 0.5 * 4 + 0.5 * 0=2，恰好等于测试时完整输出的 2。这样训练阶段已经处理了尺度，推理时就只需关闭随机屏蔽，不必再额外乘保留概率。",
      "六、为什么一会儿乘 p，一会儿除以 p？因为这两种方案选择的处理阶段不同。原始方案保持训练输出较小，在测试时把完整输出缩小；inverted 方案保持测试输出原样，在训练时把偶尔保留的输出放大。二者不是两项必须叠加的补丁。若训练已经除以 p，测试又乘 p，就再次改变了本来已经匹配的尺度。写代码时首先要弄清自己使用哪一种实现，不能看到两个公式就同时照做。",
      "七、再算一个不是一半的例子。若保留概率是 0.8，原始激活为 10，原始训练平均贡献为 8，所以原始方案测试传出 10 * 0.8=8。Inverted 方案训练保留时传出 10/0.8=12.5，屏蔽时为 0，平均仍是 0.8 * 12.5=10，测试则直接传出 10。PyTorch 的 Dropout 参数 p 表示丢弃概率，因此这个例子对应 nn.Dropout(p=0.2)，不是 p=0.8。",
      "八、匹配平均值不等于完全一样。这些计算解释了单个激活或后续线性输入的平均尺度，但不能保证训练和测试时每一次输出相同，也不保证整个非线性网络的分布完全相同。激活函数、层间相互作用和随机波动仍存在。使用完整缩放网络，也不一般等于把所有可能子网络的最终预测精确平均。现在抓住足够重要的结论：常见 inverted dropout 在训练时缩放，标准推理不再额外缩放。"
    ],
    takeaway: "两种缩放只能按一种方案配套使用：原始 Dropout 测试乘保留概率；inverted dropout 训练除以保留概率，测试保持原样。",
    formulas: [
      "Expected active inputs = p_keep * fan_in",
      "Raw training: output = mask * activation; inference: output = p_keep * activation",
      "Inverted training: output = mask * activation / p_keep; inference: output = activation",
      "Example: 0.5 * (2 / 0.5) + 0.5 * 0 = 2"
    ],
    status: "taught"
  },
  {
    page: 63,
    title: "Why Dropout Works",
    sourcePoints: [
      "第一种解释：训练过程不应过度依赖某个固定特征",
      "每次训练中，一个特征有 1-p 的概率被屏蔽，p 在本课件中是保留概率",
      "图示用被随机屏蔽的隐藏单元，说明可用特征组合不断变化"
    ],
    explanation: [
      "一、现在从怎样实现回到为什么有用。把输出随机变成零，看起来像故意破坏模型。它确实会让训练任务更困难，但困难可能推动模型形成更稳健的参数。若某个下游单元始终只依赖同一个上游特征，只要这个特征被屏蔽，它这一轮就失去关键线索。能利用其他可用信息的计算方式，才更可能在不同随机组合下都完成任务。这是本页提供的第一种直观解释。",
      "二、先解释 Feature，特征。输入图片是像素，隐藏层经过计算产生一组数值，这些数值可以表示模型发现的局部形状、纹理或其他规律。这里的特征不一定由人事先命名，也不是每个神经元只代表一个清晰概念。为了讲解，我们可以把几条通道暂时称为形状、纹理和背景通道，但那是帮助理解的示例，不是要求真实网络中的单元必然一一对应这些概念。",
      "三、为什么过度依赖会有风险？假设训练照片中的猫经常出现在沙发上。某条特征通道对沙发纹理特别敏感，模型发现只看它就能获得很低的训练误差，于是没有充分利用动物形状。遇到草地上的猫时，原先好用的线索不见了，预测就容易失败。背景特征本身不是绝对有害，问题在于把训练集里的偶然相关性当成了普遍规律。Dropout 希望减少这类脆弱依赖，但并不能保证自动识别所有错误捷径。",
      "四、屏蔽怎样改变学习压力？假设三个隐藏通道都可能提供与猫有关的信息，模型当前几乎只使用第一个。当第一个被屏蔽，而另外两个还在，模型要得到正确结果，就需要通过仍然可用的通道调整参数。如果不同轮次不断改变可用通道，训练过程就不能总假设某个特征百分之百到场。最终可能学到更分散、可替代的表示，而不是把所有判断能力押在同一个位置。",
      "五、一个简单的组合例子。原先预测所需的信息全部集中在通道 A，A 被关闭时就无法判断。经过训练后，通道 B 和 C 也可能学到一部分相关信息；A 关闭、B 保留时，模型仍有机会正确预测。这里不是说三个通道一定变成完全相同的备份，也不是说随便关闭任意数量的通道都不会影响结果。它说明的是，长期面对缺失情况，可能让模型更善于使用多种信息组合。",
      "六、与 Co-adaptation 的关系。共同适应是单元之间形成配合，配合本身并非错误，复杂任务本来就需要组合特征。我们要避免的是过于固定的配合：某个单元只有在另一个特定单元总能提供辅助信号时才有用。随机屏蔽让这种固定搭配不总能成立，因而给较不依赖特定伙伴的计算方式更多学习机会。这不是要求神经元互相独立，也不是否认深度网络逐层组合信息的价值。",
      "七、为什么训练成绩可能变差？同一张图片在不同轮次经过不同掩码，训练中的模型还要应对临时缺失的信息，因此训练损失可能比没有 Dropout 时更高。若新数据表现更好，这个代价就可能值得。评价重点是验证集，不是一定要把训练正确率推到最高。但丢弃太多也可能让有效信号不足，造成欠拟合，所以正则化强度需要结合数据量、网络容量和任务一起判断。",
      "八、不要把直观解释当成保证。Dropout 并不一定让每个特征都可靠，也不一定把所有背景捷径去掉；若训练数据本身带有系统性偏差，随机屏蔽无法替代更好的数据。对于不同网络、归一化方式和数据规模，它的收益也会不同。本页要建立的是一种学习机制的直觉：训练时让可用通道变化，可能降低对固定特征组合的过度依赖。下一页会从集成许多子网络的角度给出另一种解释。"
    ],
    takeaway: "Dropout 的第一种直觉是：关键特征并不保证每轮都在，模型因此有机会学会利用更多信息组合，减少脆弱的固定依赖。",
    formulas: [
      "P(feature kept) = p_keep; P(feature dropped) = 1 - p_keep"
    ],
    status: "taught"
  }
];
