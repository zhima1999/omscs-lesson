export const batchPages = [
  {
    page: 64,
    title: "Why Dropout Works",
    sourcePoints: [
      "解释一：模型不应过度依赖特定特征，因为一次迭代中有 1-p 的概率失去对应输出；这里 p 是保留概率。",
      "解释二：每种屏蔽配置可以看作一个网络，课件将其写成训练 2^n 个网络。",
      "课件称多数配置只用一两个 mini-batch 训练；这需要理解为对子网络采样稀疏的直观描述，而非普遍成立的训练次数。",
      "右图展示输入层、两个隐藏层和输出层，叉号表示本次被屏蔽的单元；引用 Srivastava 等人的 Dropout 论文。"
    ],
    explanation: [
      "一、本页给的是两种理解 Dropout 的视角。Dropout，随机屏蔽激活的正则化方法，在训练时让部分单元的输出暂时变成零，希望减少过拟合。左边先说不要过度依赖特定特征，再把不同屏蔽组合理解为不同网络；右图中的叉号只是本次输出不可用，并非永久删除参数。下面的计数、算例与推理说明是补充解释，不是课件报告的新实验。",
      "二、为什么特定特征突然不可用会影响学习？假设分类器只靠某个上游输出判断猫，而这个输出经常被屏蔽，下游就不能每次都用同一条信息通道完成任务。训练会推动参数尝试其他可用线索。这里的 Feature，特征，是网络内部的信息表示，不意味着每个神经元必然对应一个能用语言命名的概念；一个语义特征也可能由许多单元共同表示。因此丢掉某个输出，不等于保证彻底删除了某种背景信息。",
      "三、先算保留概率。沿用这份课件的记法，p 表示保留输出的概率，1-p 表示屏蔽输出的概率。例如 p=0.8 时，某个单元每次有 80% 的概率保留、20% 的概率关闭。若两次掩码独立，它两次都保留的概率为 0.8*0.8=0.64。p 不是预测正确率，也不要求每次恰好保留 80% 的单元；一些代码接口用 p 表示丢弃概率，使用时必须读清定义。",
      "四、为什么会出现 2^n？Mask，掩码，是记录哪些输出保留的零一数组。补充设定：有 n 个可以独立屏蔽的单元，每个都只有保留和关闭两种状态，于是配置数为 2^n。若 n=3，配置为 000、001、010、011、100、101、110、111，共八种。每种配置决定一次计算中哪些路径可用，所以可看成一个 Thinned network，稀疏化子网络。n 指参与屏蔽的单元数，不是层数，也不是所有连接的总数。",
      "五、必须修正训练 2^n 个网络的字面误解。Dropout 没有给每个子网络创建一套独立权重，更没有把 2^n 个完整模型分别训练到收敛。所有子网络从同一套网络参数中取出当前可用的部分，称为 Shared weights，权重共享。某次更新一条活跃连接的权重后，之后包含该连接的其他子网络也会使用这个新值。因此这些网络彼此关联，不能按独立训练的模型来计算参数存储量和训练成本。",
      "六、把共享过程落到一个例子。设三个隐藏输出 a1、a2、a3 接到一个输出单元，连接权重为 w1、w2、w3。掩码为 101 时，未作尺度修正的输出输入量是 z=w1*a1+w3*a3+b；掩码为 011 时则是 z=w2*a2+w3*a3+b。b 是偏置，z 是进入后续激活函数前的数值。两种配置使用的是同一个 w3；第一种配置训练后改变了 w3，第二种下次运行时也会受到影响，而不是拥有自己的 w3 副本。",
      "七、多数只训练一两个 mini-batch 应当怎样读？Mini-batch，小批量，是一次更新所用的一组样本。配置空间很大，而训练更新有限，所以很难逐一充分训练所有配置，这是课件句子的直觉。它不是数学保证：若 n 很小，同一配置可以被重复使用很多次；若 p 不等于 0.5，各配置概率也不相等。常见实现还会为同批不同样本抽不同掩码，因此一个 mini-batch 并不必然只对应一个固定子网络。严谨说法是训练中不断采样共享权重的子网络。",
      "八、Ensemble，集成，为什么有帮助？集成把多个预测组合起来，可能减轻单一模型对偶然线索的依赖。补充例子：三个分类器对猫的预测概率分别为 0.9、0.6、0.3，算术平均为 0.6；平均会平衡预测，但不会自动让答案正确。Dropout 的集成视角解释了它为什么可能具有类似作用，不过共享权重导致子网络相关，而且训练没有逐一访问所有配置，所以不能把它等同于三个独立训练分类器的简单投票。",
      "九、推理时也不是枚举全部子网络。标准推理一般关闭随机屏蔽并使用完整网络，同时保持训练和推理的激活尺度匹配。Inverted dropout，反向尺度修正的 Dropout，在训练时把保留输出除以 p。若原激活为 4、p=0.5，训练时它以一半概率输出 8、一半概率输出 0，平均贡献仍为 4。但多层非线性网络中，完整网络的最终预测通常不严格等于所有掩码预测的算术平均；尺度匹配不能被说成精确计算了 2^n 个网络的平均答案。",
      "十、这两种解释都不能当成效果保证。随机屏蔽可能减少固定依赖，也可能使信息太少、训练困难而造成欠拟合；多个单元还可能重复编码同一捷径。因此需要用独立验证数据判断强度是否合适，不能只看训练误差。把本页记成三个层次就够了：随机屏蔽改变本次信息通路，反复采样让许多共享权重的子网络参与学习，标准推理通过尺度处理利用完整网络，而不是独立训练和运行指数数量的模型。"
    ],
    takeaway: "2^n 是可选掩码配置的数量；Dropout 采样并训练共享权重的子网络，并未独立训练 2^n 个完整模型，也不保证推理是它们的精确平均。",
    formulas: [
      "PPT notation: P(keep)=p; P(drop)=1-p; 0<p<=1",
      "n independently maskable units: configuration_count=2^n; n=3 -> 8 configurations",
      "Supplement, raw mask 101: z=w1*a1+w3*a3+b; mask 011: z=w2*a2+w3*a3+b",
      "Inverted dropout: a_train=m*a/p; m is 0 or 1; a_test=a",
      "Example: p=0.5, a=4 -> mean contribution=0.5*8+0.5*0=4",
      "Supplement, three predictions: mean_probability=(q1+q2+q3)/3"
    ],
    status: "taught"
  },
  {
    page: 65,
    title: "Data Augmentation",
    sourcePoints: [
      "本页仅为 Data Augmentation 章节标题，没有新增算法、公式或实验结论。"
    ],
    explanation: [
      "一、这页负责转换主题。Data Augmentation，数据增强，指通过适当变换已有训练样本，让模型接触更多输入形式。本页只有标题；下面六段是章节导读，不是原幻灯片包含的细节。前面 Dropout 改变网络内的信息通路，接下来讨论怎样改变模型看到的数据，让学习不必过度依赖某一种固定外观。",
      "二、为什么需要更多输入形式？假设训练照片中的鸟总在图片中央，模型可能把位置当成识别线索，遇到靠边的鸟就不稳。适当平移或裁剪可以展示不同位置，这是补充教学例子。增强提供的是已有样本的变体，不是自动采集到新的独立动物，也不能补齐训练集完全没有包含的物种。",
      "三、先把输入和标签一起看。Label，标签，是模型应预测的目标，可能是类别，也可能是位置。将鸟的照片左右翻转，鸟类分类标签通常仍然成立；如果目标是判断朝左还是朝右，标签就需要改变。判断一种增强是否合理，要问变换后的样本是否仍然支持原答案，而不是只问图片看起来像不像原图。",
      "四、一个数值例子帮助理解增加的含义。假设训练集有 100 张原图，每张预先生成两个合理变体，存储条目可以变成 100*(1+2)=300。但这些条目仍然只来自 100 张原图，不是 300 次独立采集。也可以训练时临时生成变体，不保存全部图片；两种方式都不会让独立信息量简单变为原来的三倍。",
      "五、提前记住数据划分的顺序。应先按独立原始样本、必要时按人物或拍摄来源划分训练、验证和测试集，再在训练集内部增强。同一原图及其变体若分到两边，模型可能在训练中见过测试内容，造成 Data leakage，数据泄漏。验证集可用来选择增强强度，最终测试集则保留独立评价的作用。",
      "六、带着边界读下一节。后面依次介绍翻转、裁剪、颜色扰动和几何变换；不同任务允许的变化不同，例如鸟类识别常容许轻微色差，而颜色分类未必容许。补充记法 x_new=T(x) 中，x 是原输入，T 是变换，x_new 是新输入；只有目标含义保持时才沿用 y_new=y。增强要增加有意义的变化，强度过大可能制造错误监督。"
    ],
    takeaway: "这是数据增强章节的入口：改变训练输入前，先判断标签是否仍正确，并保证原始样本及其变体不会跨越训练与测试划分。",
    formulas: [
      "Supplement: x_new=T(x); if the target meaning is preserved, y_new=y",
      "Supplement example: 100 originals * (1 original + 2 variants)=300 stored entries, not 300 independent originals"
    ],
    status: "taught"
  },
  {
    page: 66,
    title: "Data Augmentation: Motivation",
    sourcePoints: [
      "数据增强是在数据上施加一系列变换，课件给 increases 一词加引号来描述数据集的扩展。",
      "变换不应改变数据的含义；如果目标含义改变，标签也必须相应改变。",
      "简单例子为 Image Flipping，图中同一张飞鸟照片进行左右镜像翻转。"
    ],
    explanation: [
      "一、课件本页的定义是什么？Data augmentation，数据增强，是对已有数据施加一系列变换，让模型在训练中看见不同版本的输入。原图中的鸟与左右翻转后的鸟，是本页展示的具体例子。这里的动机是增加训练输入的变化，使模型更可能学会任务相关规律；以下关于坐标、划分和其他任务的说明都是补充解释，不是原页新增案例或效果数据。",
      "二、为什么 increases 要加引号？将一张鸟图变成原图、翻转图和不同裁剪图，确实增加了可用训练条目，但仍然来自同一次拍摄，没有凭空得到三只不同的鸟。补充例子：200 张训练图每张生成四个变体，连同原图有 200*(1+4)=1000 个条目。它们互有关联，不能说独立样本量严格增加到五倍。训练时动态抽取变换，还可以在不同轮次看到不同版本，而不必把所有变体存成文件。",
      "三、翻转为什么可能帮助识别鸟？如果训练鸟大多朝左，网络可能把朝向与类别错误地绑定。Horizontal flip，水平翻转，把图像左右镜像，让模型也看到朝右的同一只鸟。如果任务是鸟类识别，而且朝向不影响类别，我们通常希望两个版本得到相同的类别预测。这称为 Label-preserving transformation，保持标签的变换；保持的是任务目标，并非每个像素的位置都不变，也并非所有图像任务都对翻转不敏感。",
      "四、按像素坐标算一次。补充约定：图像宽度 W=100，横坐标从 0 到 99 编号。水平翻转把原位置 x=20 变成 x_new=W-1-x=79，纵坐标 y 不变。x、y 是像素位置，W 是像素列数；W-1 是最右一列的编号，不是图片物理宽度。对于分类，目标类别 label 可以仍是鸟；对于点的位置预测，坐标标签就必须从 20 改成 79。输入和监督答案需要采用同一套变换。",
      "五、翻转也可能明确改变标签。如果任务是读文字，镜像后的文字可能无法按原文本阅读；如果任务是辨认左转与右转标志，镜像会改变方向含义；如果任务是区分左手与右手，也不能直接把镜像图当成同一目标。这些是补充反例。此时只有能定义正确的新标签、且变换后样本仍符合任务时，才适合使用对应增强；不能用数据增强总保持标签的口号覆盖具体语义。",
      "六、分类标签不动，不代表检测标签也不动。Object detection，目标检测，需要同时预测类别和边界框。设横向边界为 xmin=10、xmax=30，图宽 W=100，并采用从 0 到 W-1 的像素索引约定，翻转后的边界为 xmin_new=69、xmax_new=89。Segmentation，分割，提供每个像素的类别，其标注掩码也要左右翻转。若使用连续边界坐标或归一化坐标，公式的边界约定会不同，不能把这里的 W-1 机械复制到所有实现。",
      "七、什么样的变化值得学习？增强可以让模型更少依赖拍摄朝向等无关因素，但这是我们对任务作出的假设，必须符合未来使用场景。例如自然鸟图左右变化通常合理，严重倒置或破坏主体的处理却未必合理。Distribution，分布，是输入与目标出现的统计规律；增强应扩大合理变化范围，而不是大量生成未来根本不会出现、或答案已经不成立的样本。更多图像文件不自动代表更好的训练数据。",
      "八、如何保证 Train/test independence，训练与测试独立？先划分原始样本，再仅从训练部分生成训练增强。同一鸟图的原图在训练集、镜像图在测试集，虽然文件不同，内容仍高度重合，这属于数据泄漏。若多张照片来自同一段视频、同一主体或同一拍摄序列，还应根据评价目标按来源分组划分。需要提前固定预处理与评价规则；不能把测试图或测试答案用于训练、挑增强参数或选择表现最好的一种翻转结果。",
      "九、最后怎样验证增强的价值？固定合理的验证集，比较使用与不使用增强时的新数据表现，再调整变换概率和强度。模型训练更慢或训练正确率略低，并不自动说明增强失败；反过来，训练集得分上升也不能证明泛化改善。公式中的 T 是输入变换，g 是相应标签变换；保持标签的分类增强可以取 g(y)=y，目标变化时则要使用正确的 g。没有可靠的新标签或新图已不支持答案，就应限制或放弃该变换。"
    ],
    takeaway: "增强扩大的是合理输入变化，不是独立样本的简单倍增。翻转是否沿用标签由任务决定；位置标签要同步变换，数据必须先划分再增强。",
    formulas: [
      "Supplement: x_aug=T(x); label_aug=g(label); label-preserving classification uses g(label)=label",
      "Horizontal flip, zero-based pixel indices: x_new=W-1-x; y_new=y",
      "Example: W=100, x=20 -> x_new=79",
      "Box flip under the same index convention: xmin_new=W-1-xmax; xmax_new=W-1-xmin",
      "Example: [xmin,xmax]=[10,30] -> [69,89] when W=100",
      "Example: 200 originals * (1+4 variants)=1000 entries; independent original count remains 200"
    ],
    status: "taught"
  }
];
