export const batchPages = [
  {
    page: 79,
    title: "Inter-dependence of Hyperparameters",
    sourcePoints: [
      "课件强调超参数之间，以及网络模块的选择之间，存在相互依赖",
      "Batch norm 与 dropout 可能不必同时使用，有时组合后的效果更差；这不是绝对不兼容",
      "课件提出批大小增大时按比例增大学习率，并以梯度可能更可靠、更平滑作直觉解释",
      "学习率随批大小线性缩放是需要验证的经验规则，不能理解成所有优化器都适用的必然关系"
    ],
    explanation: [
      "一、Inter-dependence，相互依赖，表示改变一个设置之后，另一个原本有效的设置可能也需要重新选择。比如增加批大小，不只影响内存占用，也影响每轮更新次数、梯度噪声和某些归一化统计。课件的用途是提醒你把训练配置作为一个系统比较，而不是给每个旋钮永久指定最佳数值。下面的展开计算和模块放置分析都是补充解释，原页没有报告这些数值或指定唯一网络结构。",
      "二、Batch size，批大小，是一次更新用于估计梯度的训练样本数。假设仅有两个样本对同一权重的梯度分别为 2 和 6，若损失按样本取平均，批梯度就是 (2 + 6) / 2 = 4；下一组样本可能给出不同值。更大的批次在样本抽取近似独立、处理条件可比时，通常能减小这种抽样波动。这解释了课件的 smoother，更平滑，但不表示每次方向必然更正确，更不表示大批次一定泛化更好。",
      "三、为什么会想到 Linear scaling，线性缩放？在普通 SGD、平均损失、梯度在几步内变化不大的近似下，连续四次小批更新的累计改变量约为 -4 * eta * g；把四批合成一批后只有一次更新，平均梯度仍可能约为 g，于是用 4 * eta 可近似补偿更新次数减少。eta 是学习率，g 是这里假设近似不变的梯度。真实的四次小批更新会在不同参数位置计算梯度，所以这只是直觉推导，不是两条训练轨迹严格相等。",
      "四、用数字把候选规则算清楚。若旧批大小 B_old = 32，学习率 eta_old = 0.01，新批大小 B_new = 128，缩放候选是 eta_new = eta_old * B_new / B_old = 0.04。对于 1280 个样本、不考虑最后残缺批的教学设置，每轮更新从 40 次变成 10 次。0.04 是实验起点而非保证安全的答案；应与较保守的学习率比较训练稳定性和验证指标，大幅增批时还可考虑 warmup，逐渐升到目标学习率的预热。",
      "五、先检查梯度到底如何归约。Reduction，归约方式，是把多个样本损失合成一个训练目标的规则。取平均时，复制同一批数据通常不会仅因样本数翻倍就让梯度翻倍；若直接相加，梯度则可能随批大小增大，继续线性增大学习率会再次放大更新。上述缩放直觉依赖平均损失等条件。动量、学习率调度、梯度累积和训练预算也会影响结果，所以改变批大小时应重新核对这些设置，而不是只改一个数字。",
      "六、对 Adam 尤其不能机械按比例缩放。Adam 使用梯度的一阶矩 m_hat 和二阶矩 v_hat，单个参数的更新近似为 new_w = old_w - eta * m_hat / (sqrt(v_hat) + epsilon)。m_hat 是校正后的方向平均，v_hat 是校正后的平方梯度平均，epsilon 是避免分母过小的稳定项。改变批大小会改变这些统计量的噪声与演化，更新不再只由平均梯度乘一个学习率决定。因此课件那句 should 按比例改变应修正为经验候选，而不是 Adam 或所有模型中的通用定律。",
      "七、Batch normalization，批归一化，利用统计量调整激活的尺度；Dropout，则在训练时随机屏蔽激活，减少某些固定依赖。二者机制不同，不能说互为完全替代，也不能说数学上绝对不能同时出现。课件说 may not be needed together 和 sometimes worse，重点是可能冗余或效果不佳。在某个模型中单用批归一化已得到足够好的验证表现，继续加很强的 Dropout 可能让训练更难，但另一个数据条件下组合仍可能有效。",
      "八、组合效果与顺序有关。标准 BatchNorm 在训练时估计当前批次统计并更新运行统计，评估时通常使用运行统计；标准 Dropout 在训练时随机改变输出，评估时关闭这种随机性。若 Dropout 放在 BatchNorm 之前，随机屏蔽及其缩放可能改变后者看到的分布，造成训练与评估统计不匹配；影响取决于结构和强度，不能据此宣布所有排列都失败。批大小变小也可能让 BatchNorm 统计更不稳定，所以模块与超参数需要一起验证。",
      "九、怎样做一次清楚的相互依赖实验？先保留一个基准配置，再比较只用 BatchNorm、只用 Dropout、两者都用和两者都不用，并为合理候选提供可比预算及必要调参。这里的四组是补充建议，不是原页实验。Dropout 要明确参数表示丢弃概率还是保留概率；增大批次时记录样本量、更新次数与调度单位。最终按验证表现和资源约束选组合，测试集继续保持独立，不能反复用测试分数挑模块。"
    ],
    takeaway: "超参数与模块需要一起验证。批大小与学习率线性缩放只是有条件的 SGD 经验候选，尤其不能机械套给 Adam；BatchNorm 与 Dropout 并非绝对不兼容。",
    formulas: [
      "Mean gradient, two samples: g_batch = (g1 + g2) / 2 = (2 + 6) / 2 = 4",
      "SGD heuristic: eta_new = eta_old * B_new / B_old",
      "Candidate example: eta_new = 0.01 * 128 / 32 = 0.04",
      "1280 training samples: 1280 / 32 = 40 updates; 1280 / 128 = 10 updates per epoch",
      "Approximation with nearly constant g: four small SGD updates give change about -4 * eta * g",
      "Adam: new_w = old_w - eta * m_hat / (sqrt(v_hat) + epsilon)"
    ],
    status: "taught"
  },
  {
    page: 80,
    title: "Relationship Between Loss and Other Metrics",
    sourcePoints: [
      "课件指出训练优化的是损失函数，而实际关注的往往是准确率、精确率、召回率或其他任务指标",
      "这些离散判定指标通常不能直接提供适合梯度下降的导数，损失与指标的关系可能复杂",
      "图中的 relevant elements 表示真实相关元素，selected elements 表示被系统选中的元素",
      "图保留 true positives、false positives、false negatives、true negatives 四类结果；课件标注图示来源为 Wikipedia 的 Precision and recall 页面"
    ],
    explanation: [
      "一、Loss function，损失函数，是训练过程中优化的数值目标；Metric，评价指标，是我们判断任务完成得怎样的量。两者常有关联，却不等于同一个东西。分类交叉熵利用预测概率提供训练方向，而 Accuracy，准确率，只计算最终分类是否正确。课件的用途是防止你看到损失下降就宣布任务全面改善，或者看到准确率没动就认定模型没有学习。下面的垃圾邮件计数与阈值例子属于补充教学，不是图中报告的实验数据。",
      "二、为什么很多指标不能直接拿来反向传播？Threshold，阈值，是把连续分数变成离散决定的界线。若规则是概率 p >= 0.5 就判为正类，p 从 0.60 变成 0.61 时标签不变，准确率对这种局部变化通常是常数；跨过阈值时它突然跳变，在边界处也无法提供普通的平滑导数。Argmax，取最大值位置，在多分类中同样产生离散选择。这里说的是硬判定指标，不能扩大成所有指标都不可微；另有可微近似，但它们不是原始硬指标本身。",
      "三、先准确对应右图的集合。Relevant elements，真实相关元素，是按真实标签应该被找出的对象；Selected elements，选中元素，是系统实际返回的对象。左侧真实相关区域与选中圆形的交集是 TP，True Positive，真正例：该找也找到了。选中圆形中不属于真实相关区域的部分是 FP，False Positive，假正例：不该找却找到了。这里的 positive 表示系统判为目标类别，不是数值正数，也不一定表示令人满意的结果。",
      "四、另两类也要按真实标签与预测分别理解。FN，False Negative，假负例，是实际相关但没有被选中的对象，也就是漏检；TN，True Negative，真负例，是实际不相关且没有被选中的对象，也就是正确排除。True 或 false 说明这次判定是否符合真实标签，positive 或 negative 说明系统作出了哪种判定。比如把垃圾邮件设为正类，正常邮件被拦截就是 FP，垃圾邮件没被拦截就是 FN；换正类定义后，各计数的含义也随之改变。",
      "五、Precision，精确率或查准率，回答选中的对象有多少真是目标，公式是 TP / (TP + FP)。Recall，召回率或查全率，回答所有真实目标有多少被找出，公式是 TP / (TP + FN)。两者分母不同：前者按系统选择计算，后者按真实目标计算，不能互换。Accuracy 则为 (TP + TN) / (TP + FP + FN + TN)，分子是全部正确判定，分母是全部对象。四个计数共同组成 Confusion matrix，混淆矩阵。",
      "六、完整算一个补充例子。假设有 100 封邮件，其中 20 封垃圾邮件，模型拦截 15 封垃圾邮件和 5 封正常邮件。于是 TP = 15，FP = 5，FN = 20 - 15 = 5，TN = 80 - 5 = 75。精确率为 15 / 20 = 75%，召回率也为 15 / 20 = 75%，准确率为 (15 + 75) / 100 = 90%。本例两种率相同只是因为 FP 与 FN 恰好相同，不能据此认为精确率和召回率总是一样。",
      "七、为什么只看准确率可能误导？继续用 20 封垃圾邮件、80 封正常邮件的分布，模型若把所有邮件都放行，会有 80% 准确率，却一个垃圾邮件也没找到，召回率为零。若一封也不拦截，TP + FP = 0，精确率的分母为零，数学上未定义；报告时须说明采用返回零、忽略该类或其他约定。类似地，没有任何真实正类时召回率分母也为零。指标用于特定任务，必须和类别分布、错误代价一起解释。",
      "八、阈值怎样改变任务取舍？提高拦截阈值会减少或保持选中数量，通常减少误拦，但也可能漏掉更多垃圾邮件；召回率在固定分数下不会因提高阈值而增加，而精确率不保证严格单调增加。降低阈值则会找出更多或同样多的对象，同时可能带来更多 FP。若模型概率完全不变，只改决策阈值，基于这些概率的交叉熵也不变，精确率、召回率和准确率却可能变化。因此损失与指标必须各自记录，不能用其中一个推断另一个的全部变化。",
      "九、实际怎样选训练与评价目标？训练时选与任务匹配、能稳定提供梯度的损失，验证时同时看该损失及目标指标，并依据应用对 FP、FN 的代价选择阈值。阈值也是模型选择的一部分，应在验证集上选择，最终固定后再测独立测试集。多分类还要说明是每类报告、宏平均还是微平均，避免名字相同而口径不同。这页的结论不是损失没有用，而是连续训练信号与最终离散决策之间需要有清楚的评价流程。"
    ],
    takeaway: "损失提供学习方向，硬指标评价最终决策。TP、FP、FN、TN 要按真实标签与预测定义；阈值、类别分布和错误代价决定指标怎样解读。",
    formulas: [
      "TP = actual positive and predicted positive; FP = actual negative and predicted positive",
      "FN = actual positive and predicted negative; TN = actual negative and predicted negative",
      "Precision = TP / (TP + FP); Recall = TP / (TP + FN)",
      "Accuracy = (TP + TN) / (TP + FP + FN + TN)",
      "Example: TP = 15; FP = 5; FN = 5; TN = 75",
      "Precision = 15 / 20 = 0.75; Recall = 15 / 20 = 0.75; Accuracy = 90 / 100 = 0.90",
      "Binary decision: predict positive if p_positive >= tau; tau = decision threshold"
    ],
    status: "taught"
  },
  {
    page: 81,
    title: "Simple Example: Cross-Entropy and Accuracy",
    sourcePoints: [
      "课件给出单个样本的交叉熵：真实类别预测概率的负对数",
      "课件用 argmax 说明预测类别由最大概率决定；实际应对固定样本的类别索引取最大，而不是在不同样本之间取最大",
      "真实类别分数只要略高于其他类别，就能判对，因此损失变化不明显时准确率仍可能提高",
      "右侧分别标注 Loss 和 Accuracy，表达损失下降趋缓而准确率继续上升的示意关系；准确率图纵轴不可当成合法的比例测量值"
    ],
    explanation: [
      "一、这页为什么把两张图放在一起？它想说明 Cross-entropy，交叉熵，与 Accuracy，准确率，关注预测的不同方面：前者看给真实类别的概率，后者看最终选中的类别是否正确。右上 Loss 曲线下降后变缓，右下 Accuracy 曲线继续上升，但课件没有提供可复现的数据或明确横轴单位。还需修正图示：下面纵轴出现大于 1 的刻度，不能照读为准确率比例；准确率比例应在 0 到 1 之间，百分数则在 0% 到 100% 之间。",
      "二、把原公式的符号读完整。对第 i 个输入 x_i，其真实类别为 y_i，p_true = P(Y = y_i | X = x_i) 表示模型看到这个输入后给真实类别的概率。竖线表示条件为已经给定输入，不是绝对值。L_i = -ln(p_true) 是单样本损失，这里明确用自然对数 ln。公式假设一个确定的真实类别，也就是硬标签；软标签、类别权重和标签平滑会修改损失定义。以下数字都是补充计算，不能当成右图的原始数值。",
      "三、为什么负对数能指导分类学习？p_true 越接近 1，-ln(p_true) 越接近零；越接近零，损失越大。所以它不只问答对没有，还会惩罚给真实类别过低的概率，尤其是很自信地答错。它的值不是错误百分比，也不限于 0 到 1，不能把损失 0.7 解读成错误率 70%。这也解释了数值实现为何需要稳定的对数概率计算：数学上的极小概率与程序中下溢后的零并不是安全等价的输入。",
      "四、课件的 argmax 索引需要澄清。固定一个样本 x_i，让 c 遍历该任务的类别，预测为 predicted_class_i = argmax_c P(Y = c | X = x_i)，再与真实类别 y_i 比较。Argmax 返回最大值的位置或类别，不是最大概率本身。原页重复使用 i 容易让人误以为跨样本比较；批量预测时应逐个样本沿类别维度取最大。多分类判对不要求真实类别概率超过 0.5，只要求它大于所有竞争类别；概率并列时还须有固定的处理规则。",
      "五、数值例子一：很小的损失变化也能跨过决策边界。只有猫和狗两类，一张真实猫图的概率从 [0.49, 0.51] 变成 [0.51, 0.49]。猫的概率就是 p_true，损失从 -ln(0.49) 约 0.71335 降到 -ln(0.51) 约 0.67334，只下降约 0.04001，但预测从狗变成猫。这个单样本正确性从 0 跳到 1；若一百个样本中只有它发生这种变化，整体准确率只上升一个百分点，而不是上升 100 个百分点。",
      "六、数值例子二：平均损失变差，准确率仍可提高。设两个样本的真实类别都是 A，它们原先给 A 的概率为 0.90 和 0.49，在二分类 argmax 规则下一个正确、一个错误。平均交叉熵为 (0.10536 + 0.71335) / 2，约 0.40936，准确率 50%。后来概率变为 0.80 和 0.51，两者都判为 A，准确率变成 100%；平均损失却为 (0.22314 + 0.67334) / 2，约 0.44824。第一个样本虽仍正确，其信心降低带来的损失增加超过第二个样本的改善。",
      "七、仅改变阈值也能改变准确率，甚至完全不改变损失。二分类中常以 p_positive >= tau 作正类判定，tau 是决策阈值；tau = 0.5 通常对应两类 argmax，其他阈值则是额外决策选择。补充算例：一个真实正类样本的概率固定为 0.60，tau = 0.5 时判对，tau = 0.7 时判错，但 -ln(0.60) 始终约为 0.51083。改变阈值不会自动改变概率，也不是网络又做了一次学习；它改变的是概率到类别的离散映射。",
      "八、怎样准确理解课件的 flat loss curves？这里的 flat 通常指在绘图尺度上下降趋缓或整体变化不明显，不应理解成每个样本的损失都完全恒定。对单个硬标签样本，真实类别概率升高会连续降低交叉熵；准确率只有在最终判定改变时跳变。数据集平均损失还会混合不同样本的改善与退步，可能互相抵消。反过来，如果所有样本的类别都没换，概率继续向真实类别移动也会让损失下降而准确率保持不变。",
      "九、用这页建立正确的实验习惯。评估时分别记录同一数据、同一检查点的平均交叉熵和准确率，说明概率来自哪种输出、采用 argmax 还是某个阈值，不能仅凭一条曲线替另一条作结论。平均损失中的 n 是样本数量，每个 L_i 是单样本损失，不要把类别数当作这个分母。若要挑阈值或检查点，使用验证集；最后固定决策规则再测试独立测试集。训练要靠连续信号取得进展，结果要靠符合任务的指标确认，两种视角都需要。"
    ],
    takeaway: "硬标签交叉熵是 -ln(真实类别概率)，argmax 沿类别选择预测。概率与损失连续变化，准确率随决策离散跳变；平均损失和准确率没有一一对应关系。",
    formulas: [
      "One hard-label sample: p_true = P(Y = y_i | X = x_i); L_i = -ln(p_true)",
      "Prediction: predicted_class_i = argmax_c P(Y = c | X = x_i); c indexes classes, i indexes samples",
      "Three-sample mean: mean_CE = (L1 + L2 + L3) / 3",
      "Accuracy = number_of_correct_predictions / number_of_samples",
      "Example 1: -ln(0.49) = 0.71335; -ln(0.51) = 0.67334; rounded values",
      "Example 2 before: mean_CE = (0.10536 + 0.71335) / 2 = 0.40936; accuracy = 1 / 2",
      "Example 2 after: mean_CE = (0.22314 + 0.67334) / 2 = 0.44824; accuracy = 2 / 2",
      "Fixed p_positive = 0.60: CE = -ln(0.60) = 0.51083; threshold tau = 0.5 or 0.7 changes the decision"
    ],
    status: "taught"
  }
];
