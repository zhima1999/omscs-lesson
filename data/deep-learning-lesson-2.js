function taught(page, title, sourcePoints, explanation, takeaway, formulas = []) {
  return {
    page,
    title,
    sourcePoints,
    explanation,
    takeaway,
    formulas,
    status: "taught"
  };
}

export const lesson2Pages = [
  taught(
    1,
    "Neural Network View of a Linear Classifier",
    ["把线性分类器改画成神经网络形式", "输入经过带权连接得到类别分数"],
    [
      "这一页是在搭桥：我们前面学过的线性分类器，其实已经可以被画成一个最简单的神经网络。输入特征沿着连线进入输出节点，每条连线都有一个权重。",
      "所以神经网络并不是突然出现的一套全新数学。它从同一个核心动作开始：把输入乘以权重，再把结果加起来。后面的深度网络只是把这个动作重复很多次，并在中间加入非线性。"
    ],
    "先把神经网络理解成可层层组合的线性分类器。",
    ["scores = W x"]
  ),
  taught(
    2,
    "What Does a Linear Classifier Consist of?",
    ["训练样本提供输入和正确标签", "模型产生预测", "损失衡量预测有多差"],
    [
      "一个可训练系统至少有三部分：数据告诉模型要处理什么，模型把输入变成预测，损失函数把预测与正确答案比较并给出一个数。",
      "训练的目标不是直接修改答案，而是修改模型里的权重，让这个损失数字逐渐变小。只要抓住这条链条，后面看到更复杂的网络仍然是在做同一件事。"
    ],
    "训练主线是：输入经过模型得到预测，再用损失指导参数更新。"
  ),
  taught(
    3,
    "Origins of the Term Neural Network",
    ["人工神经元借用了生物神经元的连接比喻", "突触强度对应可学习权重"],
    [
      "名称来自大脑神经元，但人工神经网络只是高度简化的数学模型。一个节点接收多个数字，每个数字先乘一个权重，然后相加得到输出。",
      "权重可以理解为每条连接的重要程度。训练就是不断调整这些重要程度，让网络在任务上犯更少的错。不要把它理解成对真实大脑的完整模拟。"
    ],
    "神经网络中的学习，本质上是学习连接权重。",
    ["a = w1 x1 + w2 x2 + ... + wM xM"]
  ),
  taught(
    4,
    "Adding Non-Linearities",
    ["加权和之后加入激活函数", "示例使用 sigmoid"],
    [
      "如果节点只做加权相加，无论叠多少层，整体仍可合并成一次线性变换，表达能力没有真正增加。激活函数会把加权和再做一次非线性变换。",
      "非线性让网络能够表示弯曲边界、复杂模式和条件关系。这里的 sigmoid 把任意输入压到 0 到 1 之间，是早期网络常见的激活函数。"
    ],
    "深度带来能力的前提，是层与层之间存在非线性。",
    ["a = W x", "y = sigmoid(a)"]
  ),
  taught(
    5,
    "Connecting Many Neurons",
    ["多个输出神经元可以同时计算多个类别分数", "全连接层也叫线性投影"],
    [
      "单个神经元只能给出一个输出。把许多神经元并排放置，就能一次产生一个向量，例如猫、狗、汽车各自的分数。",
      "从矩阵角度看，每个输出神经元拥有权重矩阵的一行。一次矩阵乘法就完成所有神经元的加权求和，这就是全连接层。"
    ],
    "一层多个神经元，就是一次从输入空间到输出空间的矩阵映射。",
    ["y = f(W x)"]
  ),
  taught(
    6,
    "Neural Network Terminology",
    ["节点表示数值或激活", "边表示带权连接", "层组织同一阶段的计算"],
    [
      "图中的圆点叫节点或单元，箭头叫连接，连接上的参数叫权重。节点经过激活函数后的数值叫 activation。",
      "输入层通常不做学习，只承载数据；输出层给出最终预测；夹在中间的层叫隐藏层，因为训练数据不会直接告诉我们这些中间节点应该取什么值。"
    ],
    "记住节点、权重、激活、层和隐藏层这些基本词汇。"
  ),
  taught(
    7,
    "Connecting Many Layers",
    ["上一层的输出成为下一层的输入", "隐藏层逐步形成内部表示"],
    [
      "多层网络把简单变换串起来。第一层可能发现局部模式，下一层再把这些模式组合成更抽象的模式。",
      "理论上，一层足够宽的网络也能近似很多函数，但可能需要极多节点。多层结构常常能用更少参数自然地表达由简单规律组合而成的复杂规律。"
    ],
    "深度的价值在于逐层组合和复用中间特征。"
  ),
  taught(
    8,
    "Linear Algebra View",
    ["每一层是矩阵乘法再接激活函数", "整个网络是函数的嵌套"],
    [
      "画很多节点便于直觉理解，但真正实现时通常用矩阵。输入向量乘第一层权重矩阵，经过激活后，再乘下一层矩阵。",
      "这种写法不仅简洁，也能让 GPU 同时处理大量乘加运算。维度检查很重要：前一层输出的长度必须等于后一层权重矩阵的输入维度。"
    ],
    "网络图背后是一连串矩阵运算和非线性函数。",
    ["h1 = f1(W1 x)", "h2 = f2(W2 h1)", "y = f3(W3 h2)"]
  ),
  taught(
    9,
    "Adding More Layers",
    ["更深网络可表示复杂函数", "宽度与深度存在表达效率差异"],
    [
      "继续增加隐藏层，就得到所谓深度神经网络。每层都重新组织上一层的信息，因此最终函数虽然复杂，仍由许多简单、可微的模块构成。",
      "通用近似结论不等于一层网络在实践中最好。浅层网络为了表达某些组合结构，宽度可能大得不现实，而深层网络可以逐步完成。"
    ],
    "能表示不等于容易表示，深度常带来更高的参数效率。"
  ),
  taught(
    10,
    "Computation Graphs",
    ["章节转入计算图", "用图描述复杂函数的计算顺序"],
    [
      "计算图把一个复杂公式拆成许多简单操作。节点保存中间结果，箭头表示一个结果被哪个操作继续使用。",
      "这样做的目的不是把公式画漂亮，而是让程序能沿图向前算预测，再沿图反向自动计算每个参数的梯度。"
    ],
    "计算图是理解自动微分和反向传播的地图。"
  ),
  taught(
    11,
    "Adding Even More Layers",
    ["任意可微模块都能组合进网络", "末端损失把预测质量变成标量"],
    [
      "网络不必只由全连接层组成。卷积、归一化、激活、注意力等模块都可以连接，只要整体计算能够求导。",
      "最末端通常得到一个损失值。这个单一数字把模型在当前样本或批次上的表现概括起来，也成为反向传播的起点。"
    ],
    "现代网络是可微模块的组合，损失负责把它们连到学习目标。"
  ),
  taught(
    12,
    "Compositionality",
    ["复杂概念由简单特征逐级组合", "视觉、语音和语言都具有组合结构"],
    [
      "现实数据往往天然有层次。图像可以从像素组成边缘，从边缘组成纹理和部件，再组成完整物体；语言也可从词组成短语和句意。",
      "深度网络的层级结构与这种组合性相匹配。我们不必手工规定每层一定学什么，但训练常会形成从局部到整体、从具体到抽象的表示。"
    ],
    "深度网络擅长的问题，往往本身就具有层层组合的结构。"
  ),
  taught(
    13,
    "Computing Gradients in a Complex Function",
    ["现代网络可能有数百万到数十亿参数", "逐个手算偏导不可行"],
    [
      "要训练网络，我们需要知道每个参数变化一点会让损失怎样变化，这就是该参数的梯度。参数很多、计算路径很深时，直接展开整个公式会迅速失控。",
      "反向传播解决的是计算效率问题：复用前向过程中的中间结果和局部导数，一次反向遍历就求出全部参数的梯度。"
    ],
    "难点不是梯度的定义，而是如何高效算出海量梯度。"
  ),
  taught(
    14,
    "A General Framework",
    ["计算图是有向无环图", "模块应具有可计算的局部导数"],
    [
      "有向表示信息沿箭头流动，无环表示一次前向计算不会绕回自己。每个模块只需知道如何计算输出，以及如何根据上游梯度计算自己的局部反向结果。",
      "这种局部接口让复杂模型可以由积木式模块构成。系统不需要理解整个模型的数学含义，也能自动完成求导。"
    ],
    "把复杂函数拆成可微小模块，就能统一处理前向计算和反向求导。"
  ),
  taught(
    15,
    "A Computation Graph Example",
    ["将对数、乘法、正弦和加法拆成节点", "中间变量明确计算顺序"],
    [
      "这页用一个普通函数演示拆图。先分别计算 ln(x1)、x1 乘 x2 和 sin(x2)，再按公式组合它们。每一步都只包含一种简单运算。",
      "拆分后，向前计算时容易保存每个中间值；向后求导时，每个运算也只需提供熟悉的局部导数。"
    ],
    "复杂公式可以拆成简单操作，反向传播再把这些局部导数串起来。",
    ["f(x1, x2) = ln(x1) + x1 x2 - sin(x2)"]
  ),
  taught(
    16,
    "Machine Learning Example",
    ["线性变换、sigmoid 和对数损失组成计算图", "参数与输入都可沿图求梯度"],
    [
      "逻辑回归先用权重计算分数，再用 sigmoid 变成类别概率，最后用对数损失比较预测与标签。看似不同的机器学习步骤，在图中只是连续的可微操作。",
      "一旦损失写进计算图，自动微分就能追踪它怎样依赖每个权重。训练算法因此只需读取梯度并更新参数。"
    ],
    "模型和损失放进同一张计算图，学习就能被统一自动化。"
  ),
  taught(
    17,
    "Backpropagation",
    ["章节转入反向传播", "目标是计算损失对所有参数的梯度"],
    [
      "反向传播不是一种独立的模型，它是一种高效求导算法。它利用链式法则，从最终损失开始，把影响沿计算图反向传递。",
      "得到梯度之后，优化器才会真正修改参数。因此可以把反向传播看成负责算方向，优化器负责迈步。"
    ],
    "反向传播算梯度，优化器使用梯度更新参数。"
  ),
  taught(
    18,
    "Overview of Training",
    ["训练包含前向传播和反向传播", "两个过程都按图递归执行"],
    [
      "前向传播从输入出发，逐层得到预测和损失；反向传播从损失出发，逆序计算每个中间量和参数对损失的影响。",
      "一次前向加一次反向只完成一个训练步骤。真实训练还会把这个过程对许多批次重复，并在每次反向后更新权重。"
    ],
    "一次训练迭代的核心顺序是前向、算损失、反向、更新。"
  ),
  taught(
    19,
    "Forward Pass",
    ["按拓扑顺序计算每个节点", "输入先到隐藏层再到输出和损失"],
    [
      "前向传播只是把当前参数用于计算。某个节点的所有输入准备好后，它就执行自己的运算并把结果送往后面。",
      "此时模型还没有学习，参数也没有变化。它只是回答：按照现在这组权重，我会给出什么预测，损失是多少？"
    ],
    "前向传播负责得出当前预测和当前错误程度。"
  ),
  taught(
    20,
    "Forward Pass Through the Graph",
    ["中间节点逐个获得数值", "信息只沿箭头向输出方向流动"],
    [
      "图中的高亮过程强调依赖顺序：后面的节点不能在前面的结果尚未算出时提前执行。多分支图则可以在依赖独立时并行计算。",
      "深度学习框架会替我们安排这些操作，但读懂数据从哪来、流向哪里，是排查形状错误和理解模型的基础。"
    ],
    "沿箭头追踪张量，就能读懂一次前向计算。"
  ),
  taught(
    21,
    "Store Intermediate Values",
    ["反向传播需要前向过程的中间输出", "存储换取反向计算效率"],
    [
      "许多局部导数依赖前向时的输入或输出。例如乘法的导数要用另一个乘数，ReLU 的导数要知道输入是否大于零。",
      "所以训练通常会缓存这些中间值。这会占用显存，也是训练比单纯推理更耗内存的重要原因。某些技术会少存一些，再在反向时重算，以时间换空间。"
    ],
    "训练要记住前向路径，才能高效完成反向求导。"
  ),
  taught(
    22,
    "Backward Pass Computations",
    ["每个节点接收上游梯度", "结合局部梯度产生输入和参数梯度"],
    [
      "上游梯度表示：损失对当前节点输出有多敏感。节点再计算自己的局部导数，说明输出对各输入和参数有多敏感。两者相乘后，就得到损失对更前面变量的敏感度。",
      "节点通常要输出两类结果：传给上一层的输入梯度，以及留给优化器使用的参数梯度。"
    ],
    "反向节点做的统一动作是：上游梯度乘局部梯度。"
  ),
  taught(
    23,
    "Local Gradients",
    ["线性层示例 h_l = W h_(l-1)", "分别需要对 W 和输入求局部导数"],
    [
      "线性层的输出同时依赖权重 W 和输入 h。反向时，一条路径要回答如何调整 W，另一条路径要把梯度继续传给前一层。",
      "虽然写成完整 Jacobian 会很大，但实际实现不会显式创建它，而是直接计算梯度与 Jacobian 的乘积。"
    ],
    "每层既要为自己的参数算梯度，也要把梯度继续传回输入。",
    ["h_l = W h_(l-1)"]
  ),
  taught(
    24,
    "The Chain Rule",
    ["复合函数的导数由局部导数相乘", "链式法则连接整张计算图"],
    [
      "如果损失 L 依赖 y，而 y 又依赖 x，x 对损失的影响要经过 y 这座桥。因此 L 对 x 的导数等于 L 对 y 的导数乘 y 对 x 的导数。",
      "反向传播没有绕开微积分，它只是按计算图的结构系统地反复应用链式法则，并复用已经算出的结果。"
    ],
    "链式法则告诉我们，一处变化如何经过中间变量最终影响损失。",
    ["dL/dx = dL/dy · dy/dx"]
  ),
  taught(
    25,
    "Loss Gradients",
    ["上游梯度与局部梯度相乘", "从损失向更早的变量传播"],
    [
      "在当前节点，我们已经知道损失对节点输出的导数，这就是上游梯度；节点自己知道输出对输入的导数，这就是局部梯度。",
      "把二者相乘，便得到损失对节点输入的导数。这个结果又会成为前一个节点收到的上游梯度，于是同一种计算可以一路重复。"
    ],
    "每往回走一步，就把总影响分解为上游影响乘本地影响。"
  ),
  taught(
    26,
    "Backward Pass: First Step",
    ["从损失节点开始反向", "损失对自身的导数为 1"],
    [
      "反向传播需要一个起点。因为损失 L 对它自己变化的比例是 1，所以我们从梯度 1 开始。",
      "损失节点先把这个梯度经过自己的局部导数传给预测节点，随后图中的其他节点按逆向顺序继续。"
    ],
    "反向传播的种子梯度是 dL/dL = 1。",
    ["dL/dL = 1"]
  ),
  taught(
    27,
    "Backward Pass: Through Hidden Units",
    ["梯度依次穿过输出层和隐藏层", "每层使用前向时保存的值"],
    [
      "梯度到达隐藏层后，并不是把某个错误标签直接交给隐藏节点，而是告诉它：你的输出若变化一点，最终损失会变化多少。",
      "隐藏层利用自己的激活导数和权重，把这份责任继续分配给更早的节点。这样即使训练数据没有隐藏层标签，隐藏层也能学习。"
    ],
    "反向传播为没有直接标签的隐藏层提供学习信号。"
  ),
  taught(
    28,
    "Backward Pass: Parameter Gradients",
    ["沿途收集每组权重的梯度", "分支处的梯度会累加"],
    [
      "反向传播经过带参数的层时，会留下该层权重的梯度。它表达了当前批次下每个权重对损失的影响方向和强度。",
      "如果同一个变量通过多条路径影响损失，各条路径贡献的梯度要相加，因为总影响是所有路径影响的总和。"
    ],
    "参数梯度是所有相关计算路径贡献的合计。"
  ),
  taught(
    29,
    "Updating All Parameters",
    ["反向传播得到全部参数梯度", "梯度下降随后更新参数"],
    [
      "当反向遍历结束，每个可学习参数都有一个梯度。优化器使用学习率控制步长，通常朝负梯度方向修改权重，因为这个方向会在局部让损失下降。",
      "因此完整训练逻辑要分清两件事：backprop 计算导数，gradient descent 使用导数。"
    ],
    "算梯度与更新参数是连续但不同的两个步骤。",
    ["W <- W - learning_rate · dL/dW"]
  ),
  taught(
    30,
    "Backpropagation and Automatic Differentiation",
    ["从手工链式法则转向自动微分", "框架自动记录并反向遍历运算"],
    [
      "手工推导小网络有助于理解，但真实模型不可能逐层手写导数。自动微分系统保存前向运算形成的图，并知道每种基础操作的反向规则。",
      "调用 backward 后，系统组合这些局部规则，自动给出参数梯度。程序仍然在执行链式法则，只是由框架可靠地完成。"
    ],
    "自动微分是反向传播的工程化实现。"
  ),
  taught(
    31,
    "Reverse-Mode Automatic Differentiation",
    ["从一个标量输出反向计算许多输入梯度", "需要保存节点值和累计梯度"],
    [
      "训练通常只有一个标量损失，却有大量参数。反向模式自动微分特别适合这种一个输出、许多输入的情况，因为一次反向遍历即可求出所有参数梯度。",
      "每个节点既保存前向值，也保存已经从后方汇总来的梯度。等所有下游贡献到齐后，再把梯度传向上游。"
    ],
    "神经网络采用反向模式，是因为损失少而参数多。"
  ),
  taught(
    32,
    "Deep Learning as Differentiable Programming",
    ["程序由可微操作构成", "自动微分让复杂程序整体可训练"],
    [
      "从这个视角看，深度学习是在写一个包含可学习数字的程序。只要路径中的操作可微，最终损失就可以对这些数字求导。",
      "这解释了为何模型结构可以非常自由：卷积、分支、残差连接甚至循环都可以出现，框架会根据实际运算构建梯度路径。"
    ],
    "深度学习可以理解为用梯度训练可微程序。"
  ),
  taught(
    33,
    "Automatic Differentiation Example",
    ["为乘法、正弦和加法设置中间变量", "先执行并记录前向图"],
    [
      "示例把 f = x1x2 + sin(x2) 写成几步：乘法得到一个中间量，正弦得到另一个中间量，最后相加。",
      "每个操作都记录输入与输出。反向时，加法、乘法、正弦分别调用自己的局部求导规则，不必重新推导整个 f。"
    ],
    "自动微分依靠基础操作的导数规则拼出整个程序的导数。",
    ["u = x1 x2", "v = sin(x2)", "f = u + v"]
  ),
  taught(
    34,
    "Complete Gradient Example",
    ["x2 通过两条路径影响输出", "不同路径的梯度贡献相加"],
    [
      "x2 一方面参与乘法，另一方面进入 sin，所以它对 f 的总影响不能只看一条路径。乘法路径贡献 x1，正弦路径贡献 cos(x2)。",
      "计算图在分叉处复制前向值，在反向汇合处把梯度相加。这条规则对复杂网络中的残差连接和参数复用同样适用。"
    ],
    "一个变量多次影响结果时，总梯度等于各路径梯度之和。",
    ["df/dx1 = x2", "df/dx2 = x1 + cos(x2)"]
  ),
  taught(
    35,
    "Addition Distributes Gradients",
    ["加法把上游梯度原样传给每个输入", "多个输入分支各收到同样的梯度"],
    [
      "对于 z = x + y，z 对 x 和 y 的局部导数都为 1。因此上游梯度到达加法节点后，会原样复制到两个输入分支。",
      "反向时说复制的是梯度，而不是把梯度平均分。若输入在别处还有贡献，最终再把各处贡献累计起来。"
    ],
    "加法节点在反向中像一个梯度复制器。",
    ["dz/dx = 1", "dz/dy = 1"]
  ),
  taught(
    36,
    "Multiplication Switches and Scales Gradients",
    ["乘法对一个输入的导数是另一个输入", "数值大小会放大或缩小梯度"],
    [
      "对于 z = xy，传给 x 的梯度要乘 y，传给 y 的梯度要乘 x。也可以记成反向时交换两个乘数，再分别乘以上游梯度。",
      "这也揭示了梯度消失和爆炸的直觉：若链上反复乘小于 1 的数，梯度越来越小；反复乘较大的数，梯度可能急剧变大。"
    ],
    "乘法节点会用另一侧输入来缩放梯度。",
    ["dL/dx = dL/dz · y", "dL/dy = dL/dz · x"]
  ),
  taught(
    37,
    "Max Routes the Gradient",
    ["max 只把梯度传给前向时获胜的输入", "未被选中的路径得到零梯度"],
    [
      "如果 z = max(x, y)，输出只由较大的那一个决定。于是局部小范围内，改变较小值不会改变输出，它收到的梯度为零；获胜者收到全部上游梯度。",
      "这种路由会造成稀疏梯度。若某条路径长期得不到梯度，它就无法学习，这也是分析激活函数和池化操作时要关注的现象。"
    ],
    "max 节点把梯度送回决定前向输出的那条路径。"
  ),
  taught(
    38,
    "Computational Implementation",
    ["框架记录计算图", "每种基础运算实现 forward 和 backward"],
    [
      "工程实现中，每个运算对象知道前向怎么计算，也知道给定输出梯度后如何计算输入梯度。执行前向时，框架把这些运算连接成图。",
      "执行反向时，框架按相反顺序调用 backward 并累计梯度。这种模块化设计让新层可以由已有运算组合，也允许为特殊算子编写高效内核。"
    ],
    "自动微分框架把每个算子的局部反向规则组织成全局求导。"
  ),
  taught(
    39,
    "Forward-Mode Automatic Differentiation",
    ["前向模式沿计算方向传播导数", "成本随输入变量数量增长"],
    [
      "前向模式会选定一个输入方向，在计算数值的同时追踪该方向的导数。若只有少量输入、很多输出，它可能很合适。",
      "神经网络训练恰好相反：参数非常多而损失通常只有一个。因此逐个输入方向前推会很昂贵，反向模式更自然。"
    ],
    "选择自动微分模式取决于输入和输出数量；训练神经网络通常选反向模式。"
  ),
  taught(
    40,
    "PyTorch Dynamic Computation Graph",
    ["PyTorch 在运行代码时建立计算图", "张量操作自动记录梯度关系"],
    [
      "动态计算图意味着图不是预先画死的，而是 Python 代码执行到哪个运算，就记录哪个运算。每次前向都可以形成不同的图。",
      "只要参与计算的张量要求梯度，PyTorch 就会保留必要关系。这样模型代码读起来像普通程序，同时仍支持自动微分。"
    ],
    "PyTorch 的计算图由实际执行的张量运算动态生成。"
  ),
  taught(
    41,
    "PyTorch backward()",
    ["激活函数也进入动态图", "调用 backward 触发反向传播"],
    [
      "当代码调用 tanh 等操作时，输出张量会记住它来自哪个运算。最终对标量损失调用 backward，PyTorch 就按图逆序计算梯度。",
      "结果通常存放在叶子参数的 `.grad` 中。梯度默认会累加，所以典型训练循环在下一次反向前还要清零梯度。"
    ],
    "backward 会填充参数梯度，但更新和清零仍由训练代码负责。"
  ),
  taught(
    42,
    "Control Flow and Differentiable Programming",
    ["动态图支持条件、循环和递归", "只对本次实际执行的路径求导"],
    [
      "因为图跟随程序运行建立，模型可以包含普通控制流。循环执行几次，图中就记录几次；条件选择哪一支，反向就沿本次选中的路径走。",
      "可编程性很强，但也意味着调试时要关心实际执行路径。某条分支没有运行，就不会在这次样本中获得梯度。"
    ],
    "动态图把灵活的程序控制流与自动求导结合起来。"
  ),
  taught(
    43,
    "Logistic Regression Computation Graph",
    ["用完整分类模型复习计算图", "从输入一直连接到损失"],
    [
      "这一页开始把前面的求导工具放回真实模型。逻辑回归虽然简单，却包含线性分数、概率变换、数据损失和正则化等完整训练要素。",
      "看懂这张图后，深层网络只是在线性分数之前加入更多层，反向传播的原则完全相同。"
    ],
    "先吃透逻辑回归的计算图，再把同一逻辑扩展到深层网络。"
  ),
  taught(
    44,
    "Logistic Regression Components",
    ["输入、标签、权重、概率与损失", "正则项约束权重大小"],
    [
      "输入 x 与权重 w 先得到分数，再通过 sigmoid 得到正类概率。标签 t 告诉损失函数真实类别，交叉熵会惩罚给正确类别过低概率的预测。",
      "总目标还可加入正则项，防止权重为了迎合训练样本而变得过大。数据损失要求预测准确，正则项要求模型不过分复杂。"
    ],
    "训练目标常由拟合数据的损失与约束模型的正则项共同组成。",
    ["p = sigmoid(w^T x)", "L = cross_entropy(p, t) + regularization(w)"]
  ),
  taught(
    45,
    "Machine Learning Functions as a Graph",
    ["各模型步骤统一表示成节点", "梯度沿图回到权重"],
    [
      "数据预处理、模型、概率和损失可以统一放进一张图。只要其中要学习的路径可微，最终损失就能把训练信号送回权重。",
      "因此设计新模型时，一个实用问题是：从参数到损失之间是否存在可计算的梯度路径？若路径被不可微或离散操作切断，就需采用其他办法。"
    ],
    "可训练性的关键，是参数到损失之间有一条有效的梯度路径。"
  ),
  taught(
    46,
    "Automatic Differentiation Along the Path",
    ["自动微分组合路径上的局部导数", "参数更新无需手写整体导数"],
    [
      "损失对权重的影响会依次穿过正则项或分类损失、sigmoid 和线性层。框架在每一步使用对应局部导数。",
      "开发者主要负责正确表达前向计算和训练目标。若前向图与损失写对，框架便能给出一致的梯度，不过数值稳定性和形状仍需检查。"
    ],
    "写清前向计算，自动微分就能沿依赖路径生成反向计算。"
  ),
  taught(
    47,
    "Vectorization and Jacobians",
    ["标量链式法则扩展到向量", "Jacobian 描述向量输出对向量输入的局部变化"],
    [
      "神经网络中的变量通常是向量或矩阵，而不是单个数。此时局部导数可以组织成 Jacobian：其中每个元素表示某个输出分量对某个输入分量的导数。",
      "概念上 Jacobian 帮我们确认维度和依赖关系，实际计算却通常不显式构造巨大矩阵，而是直接完成向量与 Jacobian 的乘积。"
    ],
    "Jacobian 是向量版局部导数，但实现通常只算所需乘积。"
  ),
  taught(
    48,
    "Vectorized Chain Rule",
    ["批量线性代数代替逐元素循环", "GPU 擅长矩阵运算"],
    [
      "把许多样本和神经元放进矩阵后，一次运算就能处理整个批次。反向过程也可以写成矩阵乘法，数学上等价于大量标量链式法则。",
      "向量化减少解释器循环，并让 GPU 的并行硬件充分工作。这是深度学习能够在大数据和大模型上实际运行的重要工程基础。"
    ],
    "向量化不改变数学，只把许多相同计算高效地一起完成。"
  ),
  taught(
    49,
    "Fully Connected Layer Forward Pass",
    ["权重矩阵把上一层向量映射到下一层", "矩阵形状由输入和输出宽度决定"],
    [
      "若上一层有 D 个数、当前层有 H 个神经元，权重 W 的形状可看成 H×D。W 乘 D 维输入后得到 H 维输出。",
      "先做形状推理比背公式更重要：每个当前层神经元都要看见全部 D 个输入，所以它需要 D 个权重；H 个神经元就组成 H 行。"
    ],
    "全连接层的矩阵每一行，对应一个输出神经元的全部输入权重。",
    ["h_l = W h_(l-1)", "W: H x D, h_(l-1): D, h_l: H"]
  ),
  taught(
    50,
    "Fully Connected Layer Backward Pass",
    ["输入梯度使用转置权重", "权重梯度是上游梯度与输入的外积"],
    [
      "反向时要回答两个问题。为了继续向前一层传递责任，用 W 的转置把当前层梯度映射回 D 维；为了更新 W，把每个输出的上游梯度与每个输入值配对。",
      "完整 Jacobian 非常稀疏且庞大，但这两个直接公式可以高效得到同样结果。批次情况下再对样本贡献求和或平均。"
    ],
    "线性层反向可用两次标准矩阵运算完成，无需构造 Jacobian。",
    ["dL/dh_(l-1) = W^T dL/dh_l", "dL/dW = (dL/dh_l) h_(l-1)^T"]
  ),
  taught(
    51,
    "ReLU Layer",
    ["ReLU 保留正数并把非正数变为零", "无参数且计算便宜"],
    [
      "ReLU 对每个元素独立操作：正值原样通过，负值变成零。它给网络加入非线性，却不增加需要学习的权重。",
      "相比 sigmoid，ReLU 在正半轴不会饱和，梯度可以原样通过，因此深层网络通常更容易训练。但若输入长期为负，该单元梯度一直为零，可能成为 dead ReLU。"
    ],
    "ReLU 简单且有利于梯度流动，但负区间会完全阻断梯度。",
    ["ReLU(x) = max(0, x)"]
  ),
  taught(
    52,
    "ReLU Jacobian and Backward Pass",
    ["ReLU 的 Jacobian 是对角矩阵", "正输入导数为 1，非正输入导数为 0"],
    [
      "因为每个输出元素只依赖同位置的输入，ReLU 的 Jacobian 除对角线外都是零。实现中不需要创建矩阵，只需保存前向输入是否为正的布尔掩码。",
      "反向时，用上游梯度乘这个掩码：前向为正的位置继续传播，非正位置归零。这是元素级激活反向传播的典型模式。"
    ],
    "ReLU 反向就是用前向的正值掩码筛选上游梯度。",
    ["d ReLU(x)/dx = 1 if x > 0, otherwise 0"]
  )
];
